// A tree is a hierarchic data structure that is a colection
// of nodes, specifically where each node have its own children nodes.

class Node {
  constructor(data) {
    this.data = data;
    this.left = null;
    this.right = null;
  }
}

class Tree {
  constructor() {
    this.value,
    this.child
  }
}
